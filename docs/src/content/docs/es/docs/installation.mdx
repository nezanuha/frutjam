---
name: "Instalación"
heading: "Instalación"
description: "Instale Frutjam, la biblioteca de interfaz de usuario CSS de Tailwind solo para CSS, en minutos. Dos importaciones, cero JavaScript, componentes accesibles WCAG listos para usar. Funciona con Django, HTMX, Laravel, React y cualquier proyecto Tailwind CSS v4."
metaTitle: "Instalar Frutjam | Biblioteca de interfaz de usuario CSS Tailwind solo para CSS | Frutjam"
metaDescription: "Instale la biblioteca de interfaz de usuario CSS Tailwind exclusiva de Frutjam CSS. Dos importaciones, cero JavaScript, compatible con Tailwind v4. Funciona con Django, HTMX, Laravel y React."
order: 1
related:
  - "docs/overview"
  - "components/button"
  - "docs/configuration"
image: "https://cdn.frutjam.com/media/docs/installation.webp"
imageAlt: "Instalación - Frutjam"
createdAt: "2025-09-05T16:02:19.231238+00:00"
updatedAt: "2026-07-13T09:55:24.157638+00:00"
sourceHash: "fea80fef3124"
translationHash: "3fc9ad78d31f"
---

import Snippet from '@/components/Snippet.astro';
import RawHtml from '@/components/RawHtml.astro';
import DocTable from '@/components/DocTable.astro';

Instale Frutjam, la biblioteca de interfaz de usuario CSS de Tailwind solo para CSS, en minutos. Dos importaciones, cero JavaScript, componentes accesibles WCAG listos para usar. Funciona con Django, HTMX, Laravel, React y cualquier proyecto Tailwind CSS v4.

## ¿Cómo instalar la biblioteca Frutjam UI en el proyecto Tailwind CSS?

Asegúrese de tener instalado un proyecto [Tailwind CSS](https://tailwindcss.com/docs/installation/using-vite), así como [Node](https://nodejs.org/en/download) y NPM en su computadora. Frutjam funciona con Tailwind versión 4.

### Instale frutjam como un paquete Node ejecutando el siguiente comando:

<Snippet view="code">

```bash
npm i -D frutjam
```

</Snippet>

Añade Frutjam a tu `input.css` usando la directiva `@plugin`:

<Snippet view="code">

```css
@import "tailwindcss";
@plugin "frutjam";
```

</Snippet>

## JavaScript y React: opcional, no obligatorio

Frutjam es **primero nativo y primero CSS**. La gran mayoría de los componentes (botones, insignias, tarjetas, formularios, alertas y más) funcionan con HTML y CSS puros, sin necesidad de JavaScript. Los elementos HTML nativos como `<dialog>`, `<details>` y `<input type="checkbox">` manejan la interactividad cuando los navegadores ya la admiten. Se puede acceder a todos los componentes WCAG AA desde el primer momento y funcionan con Django, HTMX, Laravel, React, Next.js y cualquier pila.

Una pequeña cantidad de componentes no tienen equivalente HTML nativo (notificaciones de sistema, filtrado de cuadros combinados, controles de carrusel). Sólo para esos casos, Frutjam envía ayudantes opcionales livianos.

### Vainilla JS — `frutjam/js`

Asistentes de módulo ES independientes del marco para los componentes que los necesitan. Adjunte a cualquier elemento DOM, no se requiere marco.

<Snippet view="code">

```js
import { createModal } from 'frutjam/js'

const modal = createModal(document.getElementById('myModal'))
modal.open()   // calls showModal()
modal.close()  // calls close()
modal.toggle()
```

</Snippet>

### Ganchos de reacción: `frutjam/react`

Envoltorios Thin React alrededor de las mismas llamadas DOM nativas. Cada gancho devuelve un `ref` que adjuntas al elemento y métodos para controlarlo, sin dependencias adicionales más allá de React.

<Snippet view="code">

```js
import { useModal } from 'frutjam/react'

export default function App() {
  const modal = useModal()
  return (
    <>
      <button onClick={modal.open}>Open</button>
      <dialog ref={modal.ref} className="modal">...</dialog>
    </>
  )
}
```

</Snippet>

Disponible para: `useModal`, `useDrawer`, `useCollapsible`, `useTooltip`, `useTabs`, `useToast`, `useCarousel`, `useCombobox`.

### Usando una CDN

Utilice la versión CDN de la biblioteca UI de frutjam para una integración rápida y sencilla sin configuración local. Utilice esta versión CDN de frutjam sólo con fines de desarrollo, no en producción.

<Snippet view="code">

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/frutjam@2.2.5/dist/frutjam.min.css">
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
```

</Snippet>

## Próximos pasos

Una vez instalado, pruebe el componente del botón [](/components/button)para ver Frutjam en acción o explore la biblioteca completa de componentes [](/components).

<RawHtml html={"<div class=\"not-prose\"><div class=\"card card-outline card-sm mt-6\"><div class=\"card-content\"><p class=\"badge badge-secondary badge-soft mb-2\">Integración del editor de IA</p><h3 class=\"card-title mb-2\">¿Usando Claude Code, Cursor u otro editor de IA?</h3><p class=\"para-sm mb-3\">Cherry MCP lleva la biblioteca completa de componentes de Frutjam directamente al contexto de su editor de IA: nombres de clases precisos, documentos en vivo, sin copiar y pegar.</p><a href=\"/products/cherry\" class=\"btn btn-sm btn-primary\">Más información sobre Cherry MCP →</a></div></div></div>"} />

<RawHtml html={"<p class=\"mt-6\">¿Quiere comprender el pensamiento detrás del enfoque CSS-first de Frutjam? <a href=\"/blog/why-i-built-frutjam-css-first-tailwind-ui-library\">Lea por qué lo construimos de esta manera →</a></p>"} />
