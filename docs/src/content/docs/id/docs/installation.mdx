---
name: "Instalasi"
heading: "Instalasi"
description: "Instal Frutjam, pustaka Tailwind CSS UI khusus CSS, dalam hitungan menit. Dua impor, tanpa JavaScript, komponen yang dapat diakses WCAG secara langsung. Bekerja dengan Django, HTMX, Laravel, React, dan proyek Tailwind CSS v4 apa pun."
metaTitle: "Instal Frutjam | Pustaka UI CSS Tailwind Khusus CSS | buah-buahan"
metaDescription: "Instal pustaka Tailwind CSS UI khusus Frutjam CSS. Dua impor, tanpa JavaScript, kompatibel dengan Tailwind v4. Bekerja dengan Django, HTMX, Laravel, dan React."
order: 1
related:
  - "docs/overview"
  - "components/button"
  - "docs/configuration"
image: "https://cdn.frutjam.com/media/docs/installation.webp"
imageAlt: "Instalasi - Frutjam"
createdAt: "2025-09-05T16:02:19.231238+00:00"
updatedAt: "2026-07-13T09:55:24.157638+00:00"
sourceHash: "8504dece7d2c"
translationHash: "fc22fc2c1f6e"
---

import Snippet from '@/components/Snippet.astro';
import RawHtml from '@/components/RawHtml.astro';
import DocTable from '@/components/DocTable.astro';

Instal Frutjam, pustaka Tailwind CSS UI khusus CSS, dalam hitungan menit. Dua impor, tanpa JavaScript, komponen yang dapat diakses WCAG secara langsung. Bekerja dengan Django, HTMX, Laravel, React, dan proyek Tailwind CSS v4 apa pun.

## Bagaimana cara menginstal perpustakaan Frutjam UI di proyek Tailwind CSS?

Pastikan Anda memiliki pekerjaan [CSS penarik](https://tailwindcss.com/docs/installation/using-vite) proyek diinstal, serta [simpul](https://nodejs.org/en/download) dan NPM di komputer Anda. Frutjam berfungsi dengan Tailwind versi 4.

### Instal frutjam sebagai paket Node dengan menjalankan perintah berikut:

<Snippet view="code">

```bash
npm i -D frutjam
```

</Snippet>

Tambahkan Frutjam ke Anda `input.css` menggunakan `@plugin` direktif:

<Snippet view="code">

```css
@import "tailwindcss";
@plugin "frutjam";
```

</Snippet>

## JavaScript & React — Opsional, Tidak Wajib

Frutjam adalah **yang mengutamakan native dan mengutamakan CSS**. Sebagian besar komponen — tombol, lencana, kartu, formulir, peringatan, dan lainnya — berfungsi dengan HTML dan CSS murni, tanpa memerlukan JavaScript. Elemen HTML asli seperti `<dialog>`, `<details>`, Dan `<input type="checkbox">` menangani interaktivitas di mana browser sudah mendukungnya. Semua komponen adalah WCAG AA yang dapat diakses langsung dan bekerja dengan Django, HTMX, Laravel, React, Next.js, dan tumpukan apa pun.

Sejumlah kecil komponen tidak memiliki padanan HTML asli (notifikasi toast, pemfilteran kotak kombo, kontrol carousel). Hanya untuk kasus-kasus tersebut, Frutjam mengirimkan pembantu opsional yang ringan.

### vanila JS— `frutjam/js`

Pembantu modul ES framework-agnostic untuk komponen yang membutuhkannya. Lampirkan ke elemen DOM apa pun — tidak diperlukan kerangka kerja.

<Snippet view="code">

```js
import { createModal } from 'frutjam/js'

const modal = createModal(document.getElementById('myModal'))
modal.open()   // calls showModal()
modal.close()  // calls close()
modal.toggle()
```

</Snippet>

### Kait Reaksi — `frutjam/react`

Pembungkus React yang tipis di sekitar panggilan DOM asli yang sama. Setiap kait mengembalikan a `ref` Anda melampirkan elemen dan metode untuk mengendalikannya — tidak ada ketergantungan tambahan di luar React itu sendiri.

<Snippet view="code">

```js
import { useModal } from 'frutjam/react'

export default function App() {
  const modal = useModal()
  return (
    <>
      <button onClick={modal.open}>Open</button>
      <dialog ref={modal.ref} className="modal">...</dialog>
    </>
  )
}
```

</Snippet>

Tersedia untuk: `useModal`, `useDrawer`, `useCollapsible`, `useTooltip`, `useTabs`, `useToast`, `useCarousel`, `useCombobox`.

### Menggunakan CDN

Gunakan perpustakaan frutjam UI versi CDN untuk integrasi yang cepat dan mudah tanpa pengaturan lokal. Gunakan frutjam versi CDN ini hanya untuk tujuan pengembangan, bukan untuk produksi.

<Snippet view="code">

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/frutjam@2/dist/frutjam.min.css">
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
```

</Snippet>

## Langkah Selanjutnya

Setelah terinstal, coba [komponen tombol](/components/button) untuk melihat Frutjam beraksi, atau jelajahi selengkapnya [perpustakaan komponen](/components).

<RawHtml html={"<div class=\"not-prose\"><div class=\"card card-outline card-sm mt-6\"><div class=\"card-content\"><p class=\"badge badge-secondary badge-soft mb-2\">Integrasi Editor AI</p><h3 class=\"card-title mb-2\">Menggunakan Claude Code, Cursor, atau editor AI lainnya?</h3><p class=\"para-sm mb-3\">Cherry MCP menghadirkan pustaka komponen lengkap Frutjam langsung ke dalam konteks editor AI Anda — nama kelas yang akurat, dokumen langsung, tanpa salin-tempel.</p><a href=\"/products/cherry\" class=\"btn btn-sm btn-primary\">Pelajari tentang Cherry MCP →</a></div></div></div>"} />

<RawHtml html={"<div class=\"not-prose\"><div class=\"card card-outline card-sm mt-6\"><div class=\"card-content\"><p class=\"badge badge-info badge-soft mb-2\">Proyek pemula</p><h3 class=\"card-title mb-2\">Membangun dengan Django?</h3><p class=\"para-sm mb-3\">Proyek Django 6 yang berfungsi dengan Frutjam telah terhubung — akun, dasbor, tema dan contoh CRUD yang didukung penurunan harga. Kloning dan mulailah membangun.</p><a href=\"https://github.com/nezanuha/django-frutjam-starter\" target=\"_blank\" rel=\"noopener noreferrer\" class=\"btn btn-sm btn-primary\">Dapatkan starter Django →</a></div></div></div>"} />

<RawHtml html={"<p class=\"mt-6\">Ingin memahami pemikiran di balik pendekatan Frutjam yang mengutamakan CSS? <a href=\"/blog/why-i-built-frutjam-css-first-tailwind-ui-library\">Baca mengapa kami membuatnya seperti ini →</a></p>"} />
