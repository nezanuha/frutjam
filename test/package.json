{
  "name": "frutjam-test",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "npx postcss ../src/*, demo/app.css -o demo/output.css && npx serve demo",
    "build": "npm run build --prefix .. && npx postcss demo/app.css -o demo/output.css && npx serve demo"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.2.2",
    "frutjam": "file:..",
    "postcss": "^8.5.9",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
