---
name: "Toast"
heading: "Składnik tostów"
description: "Toast CSS Tailwind tylko dla CSS do pozycjonowania powiadomień — u góry, u dołu, na początku, na końcu i na środku. Brak JavaScriptu dla układu. Dostępny WCAG AA, współpracuje z Django, HTMX, Laravel, React i dowolnym stosem."
metaTitle: "Toast — komponent CSS Tailwind tylko dla CSS | Frutjam"
metaDescription: "Toast CSS Tailwind z konfigurowalnymi pozycjami i układaniem w stosy. Warianty sukcesu, błędu, ostrzeżenia i informacji dla dostępnych, nieinwazyjnych powiadomień dla użytkowników."
order: 0
related:
  - "components/alert"
  - "components/button"
image: "https://cdn.frutjam.com/media/components/toast_ds2ce1U.webp"
imageAlt: "Tosty marki Frutjam"
createdAt: "2026-04-19T15:57:25.964725+00:00"
updatedAt: "2026-07-13T09:53:05.411034+00:00"
sourceHash: "9ce87a99d8b9"
translationHash: "99552a19a16d"
---

import Snippet from '@/components/Snippet.astro';
import RawHtml from '@/components/RawHtml.astro';
import DocTable from '@/components/DocTable.astro';

Toast CSS Tailwind tylko dla CSS do pozycjonowania powiadomień — u góry, u dołu, na początku, na końcu i na środku. Brak JavaScriptu dla układu. Dostępny WCAG AA, współpracuje z Django, HTMX, Laravel, React i dowolnym stosem.

Komponenty Toast wyświetlają krótkie, nieinwazyjne powiadomienia umieszczone na krawędziach ekranu. Zbudowane ze stałych elementów pozycjonujących i alertów, tosty przekazują informacje zwrotne od systemu, takie jak potwierdzenia powodzenia, komunikaty o błędach lub aktualizacje statusu. System tostów Frutjam obsługuje sześć pozycji umieszczenia — idealnie nadaje się do przesyłania formularzy, operacji asynchronicznych i powiadomień w czasie rzeczywistym.

Tylko CSS, nie wymaga JavaScript. Dostępny w WCAG AA i niezależny od frameworka — współpracuje z Django, HTMX, Laravel, React i dowolnym stosem.

<DocTable wrapper="overflow-y-auto max-h-72" table="table table-sm not-prose">

| Klasa | Typ | Opis |
| --- | --- | --- |
| toast | <span class="badge badge-xs">Opierać</span> | Fixed-position notification container |
| toast-top | <span class="badge badge-xs badge-secondary badge-soft">Modyfikator</span> | Anchors to the top of the viewport |
| toast-bottom | <span class="badge badge-xs badge-secondary badge-soft">Modyfikator</span> | Anchors to the bottom of the viewport (default) |
| toast-start | <span class="badge badge-xs badge-secondary badge-soft">Modyfikator</span> | Anchors to the left |
| toast-center | <span class="badge badge-xs badge-secondary badge-soft">Modyfikator</span> | Poziomo wyśrodkowany |
| toast-end | <span class="badge badge-xs badge-secondary badge-soft">Modyfikator</span> | Anchors to the right (default) |
| toast-middle | <span class="badge badge-xs badge-secondary badge-soft">Modyfikator</span> | Pionowo wyśrodkowany |

</DocTable>

## Podstawowe użycie

<Snippet>

```html
<div class="toast">
  <div class="alert alert-info">
    <span>New message arrived.</span>
  </div>
</div>
```

```jsx
import { useToast } from 'frutjam/react'

export default function ToastDemo() {
  const toast = useToast()
  return (
    <>
      <button className="btn" onClick={() => toast.info('New message arrived.')}>Show Toast</button>
      <div className="toast">
        {toast.toasts.map(t => (
          <div key={t.id} className={`alert${t.type ? ` alert-${t.type}` : ''}`}>
            <span>{t.message}</span>
          </div>
        ))}
      </div>
    </>
  )
}
```

</Snippet>

## Toast Positions

### Top Positions

<Snippet>

```html
<div class="toast toast-top toast-start">
  <div class="alert alert-info"><span>Top Start</span></div>
</div>

<div class="toast toast-top toast-center">
  <div class="alert alert-info"><span>Top Center</span></div>
</div>

<div class="toast toast-top toast-end">
  <div class="alert alert-info"><span>Top End</span></div>
</div>
```

</Snippet>

### Bottom Positions

<Snippet>

```html
<div class="toast toast-bottom toast-start">
  <div class="alert alert-info"><span>Bottom Start</span></div>
</div>

<div class="toast toast-bottom toast-center">
  <div class="alert alert-info"><span>Bottom Center</span></div>
</div>

<div class="toast toast-bottom toast-end">
  <div class="alert alert-info"><span>Bottom End</span></div>
</div>
```

</Snippet>

## Toast Colors

<Snippet>

```html
<div class="toast toast-top toast-end">
  <div class="alert alert-success">
    <span>File saved successfully.</span>
  </div>
  <div class="alert alert-warning">
    <span>Storage is almost full.</span>
  </div>
  <div class="alert alert-error">
    <span>Connection failed.</span>
  </div>
  <div class="alert alert-info">
    <span>Update available.</span>
  </div>
</div>
```

</Snippet>

## Multiple Toasts

<Snippet>

```html
<div class="toast toast-top toast-end">
  <div class="alert alert-success">
    <span>Profile updated.</span>
  </div>
  <div class="alert alert-info">
    <span>2 new notifications.</span>
  </div>
</div>
```

</Snippet>
