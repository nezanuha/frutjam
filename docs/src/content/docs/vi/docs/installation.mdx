---
name: "Cài đặt"
heading: "Cài đặt"
description: "Cài đặt Frutjam, thư viện giao diện người dùng CSS Tailwind chỉ dành cho CSS, trong vài phút. Hai thành phần nhập, không có JavaScript, WCAG có thể truy cập ngay lập tức. Hoạt động với Django, HTMX, Laravel, React và bất kỳ dự án Tailwind CSS v4 nào."
metaTitle: "Cài đặt Frutjam | Thư viện giao diện người dùng CSS Tailwind chỉ dành cho CSS | trái cây"
metaDescription: "Cài đặt thư viện giao diện người dùng Tailwind CSS chỉ dành cho CSS của Frutjam. Hai lần nhập, không có JavaScript, tương thích Tailwind v4. Hoạt động với Django, HTMX, Laravel và React."
order: 1
related:
  - "docs/overview"
  - "components/button"
  - "docs/configuration"
image: "https://cdn.frutjam.com/media/docs/installation.webp"
imageAlt: "Cài đặt - Frutjam"
createdAt: "2025-09-05T16:02:19.231238+00:00"
updatedAt: "2026-07-13T09:55:24.157638+00:00"
sourceHash: "8504dece7d2c"
translationHash: "61b043fac093"
---

import Snippet from '@/components/Snippet.astro';
import RawHtml from '@/components/RawHtml.astro';
import DocTable from '@/components/DocTable.astro';

Cài đặt Frutjam, thư viện giao diện người dùng CSS Tailwind chỉ dành cho CSS, trong vài phút. Hai thành phần nhập, không có JavaScript, WCAG có thể truy cập ngay lập tức. Hoạt động với Django, HTMX, Laravel, React và bất kỳ dự án Tailwind CSS v4 nào.

## Làm cách nào để cài đặt thư viện Frutjam UI trong dự án CSS Tailwind?

Hãy chắc chắn rằng bạn có một công việc [CSS theo gió](https://tailwindcss.com/docs/installation/using-vite) dự án được cài đặt, cũng như [Nút](https://nodejs.org/en/download) và NPM trên máy tính của bạn. Frutjam hoạt động với Tailwind phiên bản 4.

### Cài đặt frutjam dưới dạng gói Node bằng cách chạy lệnh sau:

<Snippet view="code">

```bash
npm i -D frutjam
```

</Snippet>

Thêm Frutjam vào `input.css` sử dụng `@plugin` chỉ thị:

<Snippet view="code">

```css
@import "tailwindcss";
@plugin "frutjam";
```

</Snippet>

## JavaScript & React - Tùy chọn, Không bắt buộc

Frutjam là **bản địa đầu tiên và CSS đầu tiên**. Phần lớn các thành phần — nút, huy hiệu, thẻ, biểu mẫu, cảnh báo, v.v. — hoạt động với HTML và CSS thuần túy, không cần JavaScript. Các phần tử HTML gốc như `<dialog>`, `<details>`, Và `<input type="checkbox">` xử lý tính tương tác ở những nơi trình duyệt đã hỗ trợ nó. Tất cả các thành phần đều có thể truy cập WCAG AA ngay lập tức và hoạt động với Django, HTMX, Laravel, React, Next.js và bất kỳ ngăn xếp nào.

Một số ít thành phần không có HTML gốc tương đương (thông báo bánh mì nướng, lọc hộp tổ hợp, điều khiển băng chuyền). Chỉ dành cho những trường hợp đó, Frutjam cung cấp các trợ giúp tùy chọn nhẹ.

### Vanilla JS — `frutjam/js`

Trình trợ giúp mô-đun ES không xác định khung cho các thành phần cần chúng. Đính kèm với bất kỳ phần tử DOM nào - không cần khung.

<Snippet view="code">

```js
import { createModal } from 'frutjam/js'

const modal = createModal(document.getElementById('myModal'))
modal.open()   // calls showModal()
modal.close()  // calls close()
modal.toggle()
```

</Snippet>

### Móc phản ứng - `frutjam/react`

Trình bao bọc React mỏng xung quanh các lệnh gọi DOM gốc giống nhau. Mỗi hook trả về một `ref` bạn gắn vào phần tử và các phương thức để kiểm soát nó - không có sự phụ thuộc bổ sung nào ngoài chính React.

<Snippet view="code">

```js
import { useModal } from 'frutjam/react'

export default function App() {
  const modal = useModal()
  return (
    <>
      <button onClick={modal.open}>Open</button>
      <dialog ref={modal.ref} className="modal">...</dialog>
    </>
  )
}
```

</Snippet>

Có sẵn cho: `useModal`, `useDrawer`, `useCollapsible`, `useTooltip`, `useTabs`, `useToast`, `useCarousel`, `useCombobox`.

### Sử dụng CDN

Sử dụng phiên bản CDN của thư viện giao diện người dùng frutjam để tích hợp nhanh chóng và dễ dàng mà không cần thiết lập cục bộ. Chỉ sử dụng phiên bản CDN này của frutjam cho mục đích phát triển chứ không phải trong sản xuất.

<Snippet view="code">

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/frutjam@2/dist/frutjam.min.css">
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
```

</Snippet>

## Các bước tiếp theo

Sau khi cài đặt, hãy thử [thành phần nút](/components/button) để xem Frutjam hoạt động hoặc khám phá toàn bộ [thư viện thành phần](/components).

<RawHtml html={"<div class=\"not-prose\"><div class=\"card card-outline card-sm mt-6\"><div class=\"card-content\"><p class=\"badge badge-secondary badge-soft mb-2\">Tích hợp trình chỉnh sửa AI</p><h3 class=\"card-title mb-2\">Sử dụng Mã Claude, Con trỏ hoặc trình soạn thảo AI khác?</h3><p class=\"para-sm mb-3\">Cherry MCP đưa thư viện thành phần đầy đủ của Frutjam trực tiếp vào ngữ cảnh của trình soạn thảo AI của bạn — tên lớp chính xác, tài liệu trực tiếp, không cần sao chép.</p><a href=\"/products/cherry\" class=\"btn btn-sm btn-primary\">Tìm hiểu về Cherry MCP →</a></div></div></div>"} />

<RawHtml html={"<div class=\"not-prose\"><div class=\"card card-outline card-sm mt-6\"><div class=\"card-content\"><p class=\"badge badge-info badge-soft mb-2\">Dự án khởi đầu</p><h3 class=\"card-title mb-2\">Xây dựng với Django?</h3><p class=\"para-sm mb-3\">Một dự án Django 6 đang hoạt động với Frutjam đã được kết nối — tài khoản, trang tổng quan, chủ đề và ví dụ CRUD được hỗ trợ bởi markdown. Sao chép nó và bắt đầu xây dựng.</p><a href=\"https://github.com/nezanuha/django-frutjam-starter\" target=\"_blank\" rel=\"noopener noreferrer\" class=\"btn btn-sm btn-primary\">Nhận bộ khởi động Django →</a></div></div></div>"} />

<RawHtml html={"<p class=\"mt-6\">Bạn muốn hiểu suy nghĩ đằng sau cách tiếp cận CSS-first của Frutjam? <a href=\"/blog/why-i-built-frutjam-css-first-tailwind-ui-library\">Đọc lý do tại sao chúng tôi xây dựng nó theo cách này →</a></p>"} />
