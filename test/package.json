{
  "name": "frutjam-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "npx postcss demo/app.css -o demo/output.css && npx serve demo",
    "build": "npx postcss demo/app.css -o demo/output.css"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.2.2",
    "frutjam": "file:..",
    "postcss": "^8.5.8",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}